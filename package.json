{
  "name": "siskon-safety-app",
  "version": "2.0.0",
  "private": true,
  "description": "Siskon Safety Report Generator (premium rebuild)",
  "scripts": {
    "dev": "netlify dev",
    "lint": "node -e \"console.log('No linter configured (optional).')\"",
    "build": "node -e \"console.log('No build step (static in /public).')\""
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.41.0"
  }
}
